---
const { content } = Astro.props;
---

<section class="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8 lg:py-24">
  <div class="text-center">
    <h2
      class="text-balance text-3xl font-bold tracking-tight text-foreground sm:text-4xl"
    >
      {content.title}
    </h2>
    <p
      class="mx-auto mt-4 max-w-2xl text-pretty leading-relaxed text-muted-foreground"
    >
      {content.description}
    </p>
  </div>

  <div class="mt-12 flex flex-wrap items-center justify-center gap-8 ">
    {
      content.stack.map((tech: any) => (
        <div class="rounded-lg w-full max-w-48 h-48 flex flex-col items-center justify-between border border-border bg-card px-8 py-4 text-sm font-medium text-card-foreground hover:shadow-md hover:shadow-[#3e3e3e] transition-shadow">
          <img
            src={tech.icon}
            alt={tech.title}
            class="w-12 h-12 aspect-square object-cover bg-[#3e3e3e] p-2 rounded-md"
          />
          <p class="text-lg">{tech.title}</p>
          <p class="w-full text-xs text-muted-foreground text-center">{tech.description}</p>
        </div>
      ))
    }
  </div>
</section>
